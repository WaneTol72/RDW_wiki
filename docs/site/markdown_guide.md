---
sidebar_position: 4
title: Руководство по Markdown
description: Синтаксис и правила оформления документов в проекте.
---

# Руководство по Markdown

Основа — CommonMark Markdown с расширениями Docusaurus (MDX). Возможности JSX, admonitions, кастомных компонентов.

## Заголовки

Используйте # для заголовков. На странице должен быть один H1 — это заголовок документа.

# Главный заголовок страницы

## Раздел первого уровня
### Раздел второго уровня

## Адмонишины

Docusaurus поддерживает 4 типа блоков-подсказок:

:::tip
Подсказка или полезный совет.
:::

:::note
Заметка или дополнительная информация.
:::

:::danger
Опасное действие или предупреждение о последствиях.
:::

:::info
Обычная информационная заметка.
:::

С заголовком:

:::note[Важно]
Заголовок внутри блока.
:::

## Код

Блоки кода с подсветкой:

```bash
sudo apt update && sudo apt upgrade
```

Поддерживаемые языки: bash, sh, console, json, yaml, xml, html, css, javascript, typescript, python, cpp, c, java, diff и другие.

Инлайн-код: ``команда`` или ``переменная``.

## Списки

Маркированный:

- Элемент 1
- Элемент 2
  - Подэлемент

Нумерованный:

1. Первый шаг
2. Второй шаг
3. Третий шаг

## Таблицы

| Колонка 1 | Колонка 2 |
|--------|--------|
| Ячейка 1 | Ячейка 2 |
| Ячейка 3 | Ячейка 4 |

## Ссылки

Внутренние:

[Заголовок раздела](./markdown_guide)
[Оформление](./style_guide)

Внешние:

[GitHub](https://github.com)

## Выделение текста

- **Жирный**: текст
- *Курсив*: текст
- Код или ``код``

## Изображения

```![Альтернативный текст](./img/screenshot.png)```

## Горизонтальная линия

---

## Front matter

Обязательные поля для каждой страницы:

- title: Заголовок страницы
- description: Краткое описание для SEO
- sidebar_position: 1

## Комментарии в Markdown

Не отображаются на сайте:

<!-- Это комментарий -->

## Опасные символы: как писать безопасно

В технических документах часто встречаются угловые и фигурные скобки.

Правила:

1. В кодах (блоки и инлайн) скобки пишутся без экранирования.
2. В обычном тексте используйте обратные кавычки или замену.
3. В списках и описаниях, где нет риска интерпретации — можно без обратных кавычек.

Примеры:

Опасно (может быть интерпретировано как JSX/HTML):

Передайте параметр ``<``user``>``.
Используйте ``{``variable``}``.

Безопасно:

Передайте параметр ``<user>``.
Используйте ``{variable}``.

Или замените на &lt; и &gt;:

&lta;&gt; &lta href="x"&gt;ссылка&lgtilde;

В кодах можно писать как угодно:

```bash
ssh user@host -o "UserKnownHostsFile /dev/null"
curl -X POST -H "X-Token: {token}" https://api.example.com
```

При работе с конфигами, шаблонами, JSON/YAML — используйте блоки кода:

```json
{
  "name": "{project}",
  "path": "<root>/src"
}
```

## Дополнительно

- Пустые строки между блоками улучшают читаемость исходника.
- Короткие строки (до ~100 символов) и логичные разрывы помогают при правках.
- Единый стиль — без смешивания кавычек, пробелов и табов.

---

См. также: [стандарты оформления](./style_guide), [пример документа](./style-example).